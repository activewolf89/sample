import React from 'react';
import Div from 'shared/Div';
import {H2} from 'shared/H';

// -----
const funcC0mp = (props) =>{
  return (
    <Div padding='20px'>
      <H2 textAlign='center'>
        The URL you copied is incorrect.
        Re-copy the link and paste it again, or click on sent Email.
      </H2>
  </Div>
  )
}
funcC0mp.propTypes = {
}
export default funcC0mp;
